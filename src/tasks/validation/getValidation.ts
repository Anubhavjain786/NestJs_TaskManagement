import { GetTaskFilterDto } from '../dto/get-tasks-filter.dto';

export function getValidation(inputs, type) {
  switch (type) {
    case 'getTasks': {
      // let obj = new GetTaskFilterDto();
      // obj.search = inputs.search;
      // obj.status = inputs.status;
      // return obj;
    }
  }
}
